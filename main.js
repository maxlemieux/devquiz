// Define different views for the app
var views = ["intro", "quiz", "completed", "highscores"];
// Set initial view to intro
var currentView = views[0];

// Create top bar with high scores link on left, timer on right

// Intro text with start button